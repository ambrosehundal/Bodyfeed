
<h1>Meetup Events near me</h1>

<table class="table-bordered table table-hover table-dark table-marg">
  <thead>
    <tr>
      <th>Name</th>
      <th>Status</th>
      <th>Date</th>
      <th>Local Time</th>
    </tr>
  </thead>

  <tbody>
    <% @events.each do |event| %>
      <tr>
        <td><%= event["name"] %></td>
        <td><%= event["status"]%></td>
        <td><%= event["local_date"] %></td>
        <td><%= event["local_time"] %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

