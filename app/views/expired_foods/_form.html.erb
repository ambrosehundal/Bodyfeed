<%= form_with(model: expired_food, local: true) do |form| %>
  <% if expired_food.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(expired_food.errors.count, "error") %> prohibited this expired_food from being saved:</h2>

      <ul>
      <% expired_food.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :email %>
    <%= form.text_field :email %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
